Password recovery

router 1, power off then on. After, change register to 
restart without running configuration.

you can break after power off to go into ROM mode and do this.
Easier that way.

to ignore NVRAM: 0x2142 (just remember the common vals)

so if you do this, you will have a default config, and may see the password if unencrypted. However, you also do not need to see it since you recovered the config.
Just do:
- copy startup-config running-config

-> meaning: if you don't know device password, you can bypass configuration by setting register to 0x2142.. rebooting, doing enable, copying config, and setting password. Though you may need to set it back to some normal register like 0x2102

NOTE:
USUALLY, IF YOU HAVE SPECIFIC STARTUP CONFIG YOU NEED, YOU GO TO PRIVILEGED MODE (enable mode) FIRST. THEN YOU CAN COPY.

you can override in this mode by writing enable secret (number) val (of password secret)

!once you set config register, no need to save config (no need to do copy running-config startup-config)

 PASSWORD RECOVERY ON A SWITCH (3560)
 -> 1. enabled password recovery 
 2. disabled

 for enabled,


 connect a switch and make a terminal session
(also physically done) or just use switch cli

- 9600 badu line speed required (default in cpt)

- power off switch

- press mode button within 15 seconds while LED is green

-> when led is briefly amber and then solid green release mode button.

- you can emulate this process via "power cycle button", then on switch you click mode button..

- gotta press it for 3 seconds or 3 times

- ROM MON MODE ENABLED

(in real life you would do flash_init, but cpt does that instead)

- do dir flash:

- rename flash:config.text flash:config.text.old 

- now "boot" command (for initial configuration dialog)

-> go priveleged and rename config.text.old (like before opposite ways)

- in cpt you need to use gui to erase nvram. You will still have password in flash to recover..

- directly you get to privilege in cpt but in real life you dont need to have extra step (tho no need to do power cycle)


the rest now is easy:
S1#erase startup-config (use GUI easier..)
Erasing the nvram filesystem will remove all configuration files! Continue? [confirm]N
S1#
S1#copy flash ru
S1#copy flash running-config 
Source filename []? config.text.old
Destination filename [running-config]? 

1306 bytes copied in 0.416 secs (3139 bytes/sec)
S1#
%SYS-5-CONFIG_I: Configured from console by console

